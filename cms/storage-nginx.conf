# Agregar esto dentro del bloque server{} en tu configuración nginx
# Bloquea ejecución de PHP en la carpeta de storage pública

location ~* ^/storage/.*\.(php|phtml|phar|pl|py|cgi|sh)$ {
    deny all;
    return 403;
}
